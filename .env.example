# Required. Get one at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Strongly recommended. Without it GitHub allows 60 requests/hour per IP,
# which Shipped burns through in about two repos. A classic token with no
# scopes at all is enough — we only read public data.
# https://github.com/settings/tokens
GITHUB_TOKEN=
